# Configuração local não-secreta.
#
# O Prisma CLI lê `.env` (não `.env.local`), então o DATABASE_URL precisa estar
# aqui para `npm run db:push` / `db:seed` / `db:studio` funcionarem.
# Segredos ficam em `.env.local` (ignorado pelo git), que tem precedência.
DATABASE_URL="file:./dev.db"
