# Dependências e build
.next/
node_modules/
out/
build/

# Segredos — `.env` é versionado de propósito (só tem o DATABASE_URL local,
# não-secreto, e o Prisma CLI precisa dele). Segredos vão em `.env.local`.
.env.local
.env*.local
.env.production
.env.development

# Banco local
*.db
*.db-journal
*.sqlite
*.sqlite3

# Runtime Node vendorizado (não faz parte do projeto)
node_runtime/
node.zip

# Sistema / editor
.DS_Store
Thumbs.db
*.log
npm-debug.log*
.vscode/
.idea/

# TypeScript
*.tsbuildinfo
next-env.d.ts
